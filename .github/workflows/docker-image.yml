name: Docker Image CI

on:
  push:
    branches: [ "patch-2" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag sophware01/tsbastionazure:$(date +%s)
